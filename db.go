package main

//"context"

//"go.mongodb.org/mongo-driver/mongo"
//"go.mongodb.org/mongo-driver/mongo/options" //may need to add /readpref

func initDb() {

	//What is context
	//How do I run a local Mongo instance?
	//What is being connected to below?
	// Connect to MongoDB instance, in future these may be seperate instances
	// This is also where authentication can be configured in the future
	//client, err := mongo.Connect(context.TODO(), options.Client().ApplyURI("mongodb://localhost:27017"))
	//if err != nil {
	//	panic(err)
	//}
}
